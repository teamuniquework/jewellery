// import { get } from "../../server/category";

export default async function Home() {

  // const data = await get();
  // return (
  //   <div>
  //     {data?.map((categories) => (
  //       <p key={categories.id}>{categories.name}</p>
  //     )) || <p>No categories available</p>}
  //   </div>
  // );
}
